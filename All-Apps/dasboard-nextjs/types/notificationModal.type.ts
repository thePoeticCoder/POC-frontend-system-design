import { voidFunctionType } from './common.types'

type NotificationModalType = {
  toggleModalDisplay: voidFunctionType // TODO: Type must begin with uppercase
}

export type { NotificationModalType }
